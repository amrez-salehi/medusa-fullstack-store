"use client"
import { useEffect, useState } from "react"
import AdminShell from "@modules/admin/components/admin-shell"
import { adminSdk, toman } from "@modules/admin/lib/sdk"
type Order = { id: string; display_id?: number; email?: string; payment_status?: string; fulfillment_status?: string; total?: number; currency_code?: string; created_at?: string }
export default function AdminOrders() { const [orders, setOrders] = useState<Order[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); useEffect(() => { adminSdk.admin.order.list({ limit: 100, offset: 0 }).then((d) => setOrders((d.orders || []) as Order[])).catch(() => setError("دریافت سفارش‌ها انجام نشد.")).finally(() => setLoading(false)) }, [])
  return <AdminShell title="سفارش‌ها" description="پیگیری وضعیت پرداخت و ارسال سفارش‌های مشتریان."><section className="admin-card"><div className="admin-section-head"><div><h2 className="admin-section-title">آخرین سفارش‌ها</h2><p className="admin-section-note">فهرست زنده سفارش‌ها از API مدوسا</p></div></div>{loading ? <div className="admin-loading">در حال دریافت سفارش‌ها…</div> : error ? <div className="admin-error">{error}</div> : orders.length === 0 ? <div className="admin-empty">هنوز سفارشی ثبت نشده است.</div> : <div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>شماره سفارش</th><th>مشتری</th><th>پرداخت</th><th>ارسال</th><th>مبلغ</th><th>تاریخ</th></tr></thead><tbody>{orders.map((o) => <tr key={o.id}><td><strong>#{o.display_id}</strong></td><td>{o.email || "بدون ایمیل"}</td><td><span className="admin-status">{o.payment_status || "در انتظار"}</span></td><td className="admin-muted">{o.fulfillment_status || "در انتظار"}</td><td>{toman(o.total || 0)}</td><td className="admin-muted">{o.created_at ? new Date(o.created_at).toLocaleDateString("fa-IR") : "—"}</td></tr>)}</tbody></table></div>}</section></AdminShell> }
